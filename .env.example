# PostgreSQL Config (from base_infra)
DATABASE_URL=postgresql://zenconnect_app:change_me_zenconnect_secret@postgres:5432/meowdb

# Redis Config (from base_infra)
REDIS_URL=redis://:change_me_redis_secret@redis:6379/1

# Webhook Auth
CONVERSATIONS_WEBHOOK_SECRET=your_webhook_secret_here

# Zendesk Conversations API Auth
SUNCO_APP_ID=your_sunco_app_id
SUNCO_KEY_ID=your_sunco_key_id
SUNCO_KEY_SECRET=your_sunco_key_secret
INTEGRATION_KEY_ID=your_integration_key_id
INTEGRATION_KEY_SECRET=your_integration_key_secret

# Zendesk Tickets API
ZENDESK_SUBDOMAIN=kasikornbankgroup
ZENDESK_EMAIL=admin@example.com
ZENDESK_API_TOKEN=your_zendesk_api_token
ZENDESK_AGENT_GROUP_ID=your_target_group_id_for_handoff

# RAG Integration
RAG_BASE_URL=http://rag-api:8001
RAG_API_KEY=your_rag_api_key_here

# Flush Buffer Debounce (seconds)
FLUSH_BUFFER_DEBOUNCE_SECONDS=30

# AI Working Hours (agent replies only during this window)
AGENT_TIMEZONE=Asia/Bangkok
AGENT_WORKING_DAYS=[0,1,2,3,4]
AGENT_WORKING_HOUR_START=9
AGENT_WORKING_HOUR_END=18
# AGENT_OUTSIDE_HOURS_REPLY=ขออภัย เราให้บริการวันจันทร์-ศุกร์ เวลา 09:00-18:00 น.

# Concurrency tuning
# API: WEB_CONCURRENCY uvicorn workers × DB_POOL_MAX_SIZE = total API DB connections
# Worker: replicas × WORKER_POOL_MAX_SIZE = total worker DB connections
#         replicas × WORKER_MAX_JOBS = max concurrent AI reply slots
WEB_CONCURRENCY=4
DB_POOL_MAX_SIZE=5
WORKER_POOL_MAX_SIZE=10
WORKER_MAX_JOBS=10

# App Environment
ENV=development

# OpenTelemetry (traces → Jaeger via infra base)
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4317
